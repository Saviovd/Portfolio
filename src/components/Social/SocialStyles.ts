import styled from 'styled-components';

export const SocialStyle = styled.div`
   display: flex;
   flex-direction: column;
   gap: 2rem;

   position: absolute;
   right: 0;

   .social {
      width: 90px;
      height: 90px;
   }
`;
